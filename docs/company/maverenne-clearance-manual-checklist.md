# Maverenne 人工核验清单（约 10 分钟）

**目的：** 由获授权的账号管理员自行完成并留存第一方证据，补齐 `Maverenne` 的名称、域名和社交用户名核验。

**边界：** 本清单不是法律意见，也不是购买、申请或改名授权。执行人不得购买或预订域名、提交商标申请、变更账号资料或发布内容。任何无法确认的结果均记录为 `not_available` 或 `fail`，不得写成通过。完成清单本身**不改变** [maverenne-name-clearance.md](maverenne-name-clearance.md) 的 `name_clearance_passed: false`；仅有律师复核并完成所需审批后才能改变门槛。

## 记录页

在开始前创建一个仅限内部访问的证据文件夹，并为每步填写：

| 字段 | 填写内容 |
| --- | --- |
| 检查时间 | 带时区的 ISO 8601 时间，例如 `2026-07-26T09:30:00+08:00` |
| 执行人 | 账号管理员姓名或内部角色 |
| 官方 URL | 本清单中的实际查询 URL |
| 查询词 / 用户名 | 实际输入的词或 `maverenne` |
| 结果 | `pass`、`fail` 或 `not_available` |
| 截图路径 | 内部证据文件的稳定路径；截图需显示 URL、查询词/用户名、时间及结果 |
| 备注 | 记录类别、商品/服务描述、所有人、价格或异常提示；不要记录密码、付款信息或令牌 |

## 1. USPTO 商标搜索（约 3 分钟）

- 官方 URL：[USPTO Trademark Search](https://tmsearch.uspto.gov/search/search-information)
- 查询顺序：精确词 `Maverenne`，再查 `Maverene`、`Maveren`、`Maverenn`、`Maverinne`。
- 对每个命中审阅并记录：存续/待审状态、文字/图形标记、所有人、商品或服务描述，以及 Nice 第 `14`、`18`、`26`、`35` 类是否相关。
- 截图要求：每个查询词至少一张完整结果截图；对相关命中另保存详情页截图。截图必须保留浏览器地址栏。
- `pass` 记录条件：所有上述词均已可复查地查询，并且没有由非法律人员可明显识别的、在相关类别中高度相似的在先珠宝/配饰/零售标记；仍须提交商标律师复核。
- `fail` / 停止条件：发现相关类别的高度相似在先标记，或无法完成任一查询。停止品牌迁移，保留 `name_clearance_passed: false`，交律师/命名负责人处理；不得靠改拼写继续。

## 2. WIPO Global Brand Database（约 2 分钟）

- 官方 URL：[WIPO Global Brand Database](https://branddb.wipo.int/en/quicksearch)
- 查询顺序：`Maverenne`、`Maverene`、`Maveren`、`Maverenn`、`Maverinne`。
- 如系统要求验证码或登录，由执行人本人按其平台权限完成；不要绕过验证。
- 截图要求：每个查询词的结果页，及每个相关命中的详情页；需显示查询词、URL 与结果数/空结果。
- `pass` 记录条件：每个查询有可复查结果，且未见明显相关的高度相似珠宝、配饰或零售标记；同时标记“待律师国际范围复核”。
- `fail` / 停止条件：发现相关高度相似记录，或验证码/访问限制使结果无法可靠确认。记录 `not_available`（访问限制）或 `fail`（冲突），停止迁移放行。

## 3. 域名注册商实时确认（约 1 分钟）

- 官方 URL：使用公司已批准注册商的官方域名搜索页；若尚未指定注册商，记录 `not_available`，不要以 RDAP 或第三方 WHOIS 代替。
- 查询词：`maverenne.com`
- 截图要求：搜索结果页，必须显示完整域名、可注册/不可注册状态、首年价格、续费价格、溢价或保留提示和查询时间。
- `pass` 记录条件：该注册商明确显示域名可注册，且无保留、溢价或其他提示未被记录；这仅是可购性证据，**不等于购买授权**。
- `fail` / 停止条件：显示已注册、保留、不可用、价格/条款无法确认，或未指定获批注册商。不要加购、结账、预订或支付。

## 4. Pinterest 用户名（约 1 分钟）

- 官方 URL：[Pinterest](https://www.pinterest.com/)
- 查询用户名：`maverenne`；由账号管理员在其自身会话中使用 Pinterest 的官方创建/改名界面仅查看可用性，不保存任何更改。
- 截图要求：显示用户名输入框、`maverenne`、平台明确的可用/不可用提示和 URL；不要截图个人资料、通知或私信。
- `pass` 记录条件：平台明确提示该用户名可用，且未提交保存。
- `fail` / 停止条件：平台提示已占用/不允许、出现近似官方主体，或状态未明确。记录 `not_available` 而非猜测。

## 5. TikTok 用户名（约 1 分钟）

- 官方 URL：[TikTok](https://www.tiktok.com/)
- 查询用户名：`maverenne`；由账号管理员在其自身会话中打开官方账号设置中的用户名校验，仅查看、不保存。
- 截图要求：显示输入的用户名、平台提示、URL 和时间；排除账号邮箱、手机号与私信。
- `pass` 记录条件：平台明确显示用户名可用，且没有提交账号变更。
- `fail` / 停止条件：已占用、受限、相似官方主体、或结果不明确/不可访问。记录实际状态并停止放行。

## 6. Instagram 用户名（约 1 分钟）

- 官方 URL：[Instagram](https://www.instagram.com/)
- 查询用户名：`maverenne`；由账号管理员在其自身会话的官方资料编辑界面只查看用户名可用性，不提交。
- 截图要求：显示用户名字段、`maverenne`、明确平台提示、URL 和时间；遮盖或避免包含个人资料信息。
- `pass` 记录条件：平台明确显示用户名可用，且未保存资料变更。
- `fail` / 停止条件：已占用、受限、可见近似官方主体，或无法得到明确平台提示。写 `not_available` 或 `fail`，不得推测可用。

## 收尾判定（约 1 分钟）

1. 把所有记录和截图路径附到 [maverenne-name-clearance.md](maverenne-name-clearance.md) 的相应证据项，供律师和命名负责人复核。
2. 任一项 `fail`、`not_available`、缺失截图或未完成律师复核时：保持 `name_clearance_passed: false`，停止后续迁移任务。
3. 即使各项都暂记 `pass`，也不得购买、申请、改账号或上线；仍需法律复核及独立的购买/生产授权。
